package Array;

import com.sun.org.slf4j.internal.Logger;
import com.sun.org.slf4j.internal.LoggerFactory;

import java.util.Arrays;


public class RemoveDuplicates_IntegerArray {
    /*
    public static void main(String[] args) {
        int[][] test = new int[][]{
                {1, 1, 2, 2, 3, 4, 5},
                {1, 1, 1, 1, 1, 1, 1},
                {1, 2, 3, 4, 5, 6, 7},
                {1, 2, 1, 1, 1, 1, 1},};

        for (int[] each : test) {
            System.out.println("Array with Duplicates : " + Arrays.toString(each));
        }
        System.out.println("After removing duplicates : " + Arrays.toString(RemoveDuplicates(each)));
    }
}

   public static int[] removeDuplicates(int[] numbersWithDuplicates) {
       //Sorting array to bring duplicate together
       Arrays.sort(numbersWithDuplicates);

       int[] result = new int[numbersWithDuplicates.length];
       int previous = numbersWithDuplicates[0];
       result[0] = previous;

       for (int i = 1; i < numbersWithDuplicates.length; i++) {
           int ch = numbersWithDuplicates[i];

           if (previous != ch) {
               result[i] = ch;
           }
           return result;
       }

     */
   }
